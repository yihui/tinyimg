% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinyjpg.R
\name{tinyjpg}
\alias{tinyjpg}
\title{Optimize JPEG images}
\usage{
tinyjpg(
  input,
  output = identity,
  quality = 75L,
  recursive = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{input}{Path to the input JPEG file or directory. If a directory is
provided, all JPEG files in the directory (and subdirectories if
\code{recursive = TRUE}) will be optimized.}

\item{output}{Path to the output JPEG file or directory, or a function that
takes an input file path and returns an output path. When optimizing a
directory, \code{output} should be a directory path or a function.}

\item{quality}{Quality level (0-100). Higher values preserve more detail
but result in larger files. Default 75 is a good balance between quality
and file size.}

\item{recursive}{When \code{input} is a directory, recursively process
subdirectories.}

\item{verbose}{Print file size reduction info for each file.}
}
\value{
Character vector of output file paths (invisibly).
}
\description{
Optimize JPEG files or directories of JPEG files by re-encoding them with
\href{https://github.com/mozilla/mozjpeg}{mozjpeg}'s optimized Huffman coding
and quantization tables.
}
\details{
Re-encoding at the specified \code{quality} level produces a file that is often
considerably smaller than the original without a perceptible quality loss
when \code{quality >= 75}. Lower values shrink files further at the cost of
visible JPEG artefacts.
}
\examples{
# Create a test JPEG
tmp = tempfile(fileext = ".jpg")
jpeg(tmp, width = 400, height = 400)
plot(1:10)
dev.off()

# Optimize with default quality
tinyjpg(tmp)

# Optimize to a new file at a specific quality
tinyjpg(tmp, paste0(tmp, "-q60.jpg"), quality = 60)
}
\seealso{
\code{\link[=tinyimg]{tinyimg()}} to optimize both PNG and JPEG files at once.
}
