% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinyimg.R
\name{tinyimg}
\alias{tinyimg}
\title{Optimize images (PNG and JPEG)}
\usage{
tinyimg(
  input,
  output = identity,
  recursive = TRUE,
  verbose = TRUE,
  level = 2L,
  quality = 75L,
  ...
)
}
\arguments{
\item{input}{Path to an image file, a character vector of image file paths,
or a directory. Supported formats: \code{.png}, \code{.apng}, \code{.jpg}, \code{.jpeg}.}

\item{output}{Path to the output file or directory, or a function that
takes an input file path and returns an output path. When \code{input} is a
directory or a vector, \code{output} should be a directory path or a function.}

\item{recursive}{When \code{input} is a directory, recursively process
subdirectories.}

\item{verbose}{Print file size reduction info for each file.}

\item{level}{Passed to \code{\link[=tinypng]{tinypng()}} as the PNG optimization level (0-6).}

\item{quality}{Passed to \code{\link[=tinyjpg]{tinyjpg()}} as the JPEG quality (0-100).}

\item{...}{Additional arguments passed to both \code{\link[=tinypng]{tinypng()}} and \code{\link[=tinyjpg]{tinyjpg()}}.}
}
\value{
Character vector of output file paths (invisibly).
}
\description{
Optimize image files or a directory of image files. PNG files are optimized
via \code{\link[=tinypng]{tinypng()}} and JPEG files via \code{\link[=tinyjpg]{tinyjpg()}}. This is the recommended
entry point when you have a mix of image types or do not know the exact
formats in advance.
}
\examples{
# Create test images
tmp_png = tempfile(fileext = ".png")
png(tmp_png, width = 400, height = 400)
plot(1:10)
dev.off()

tmp_jpg = tempfile(fileext = ".jpg")
jpeg(tmp_jpg, width = 400, height = 400)
plot(1:10)
dev.off()

# Optimize both in one call
tinyimg(c(tmp_png, tmp_jpg))

# Optimize all images in a directory
d = tempfile()
dir.create(d)
file.copy(tmp_png, file.path(d, "plot.png"))
file.copy(tmp_jpg, file.path(d, "photo.jpg"))
tinyimg(d)
}
\seealso{
\code{\link[=tinypng]{tinypng()}} for PNG-specific options, \code{\link[=tinyjpg]{tinyjpg()}} for JPEG-specific
options.
}
